# build output
dist/
.output/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

pnpm-lock.yaml

.astroexperiments/raw/

# astro generated types
.astro/

# ── Citonyx machinery — ПРИВАТНО (внутренняя механика сбора/измерения/публикации) ──
# Наружу нельзя: runner (как меряем), bridge/digest (как публикуем), prompts (панель запросов).
# Публичны только ДАННЫЕ: experiments/runs/*.json + schema. Полное удаление из истории — отдельно (purge).
experiments/*.py
experiments/*.yaml
experiments/*.sh
experiments/README.md
experiments/digest_prompt.md
experiments/review_prompt.md
experiments/requirements.txt
experiments/sprints/
